node {
    def app

    stage('Clone repository') {
        checkout scm
    }

    stage('Build image') {
        app = docker.build("staging/grubstack-core", "-f ./deployment/staging/Dockerfile .")
    }

    stage('Test image') {
        app.inside {
            sh 'echo "Tests passed"'
        }
    }

    stage('Push image') {
        docker.withRegistry('https://docker.grubstack.app', 'grubstack-staging-credentials') {
            app.push("latest")
        }
    }
}